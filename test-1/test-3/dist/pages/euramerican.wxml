
  <view class="container">
    <view class="photo">
    <view class="e_goods">
      <view class="egoods"  wx:for="{{euramerican}}" wx:key="index">
        <image class="e_pic" src="{{item.pic}}" id="{{index}}" bindtap="lagoods"/>
        <text class="e_introduce">{{item.title}}</text>
        <text class="e_price">{{item.price}}</text>
        <text class="e_number">{{item.number}}</text>
        <image class="e_collect" src="{{item.acollect ? '../images/bcollect.png' : notcollect}}" id="{{index}}" bindtap="collect"/>
      </view>
     </view> 
  </view>
  </view>